# setup of a new system [MacOS, Linux]
#
# set up github access keys and clone this repo to ~/
# Install powerline patched Monaco font on system
# change terminal emulator font to  Monaco for Powerline
# Install oh-my-zsh, change default shell to ZSH
# install zsh-vi-mode plugin:
#   git clone https://github.com/jeffreytse/zsh-vi-mode \
#   $ZSH_CUSTOM/plugins/zsh-vi-mode
# Then enable this plugin by adding to plugins in .zshrc
# change oh-my-zsh theme to `agnoster`
# install tmux
# link ~/.tmux.config to this repo's version
# link ~/.vimrc to this repo's version
# link ~/.vim/colors to this repo's version
# source this repo's zshrc in the actual .zshrc
